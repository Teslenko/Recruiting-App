
<div class="input-group-addon">
  <p>
    <strong>The Title Of The Article:</strong>
    <%= @article.title %>
  </p>

  <p >
    <strong>Description Article:</strong>
    <%= @article.text %>
  </p>
</div>

<div class="input-group-addon">
<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article), class: "btn btn-large btn-primary" %> |
<%= link_to 'Back to Home', articles_path, class: "btn btn-large btn-primary" %>
</div>